# Windows signing — used by dist:win, publish:win, dist:all
# Kept in a separate file from .env so a Mac build never loads these
CSC_LINK=/path/to/your/certificate.pfx
CSC_KEY_PASSWORD=your-cert-password

# GitHub — needed for publish:win to actually upload the build to a Release
GH_TOKEN=your-github-personal-access-token
